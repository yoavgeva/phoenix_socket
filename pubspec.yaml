name: phoenix_socket
description: A lightweight Phoenix Channel V2 client for Dart using web_socket_channel and Streams.
version: 0.1.0
repository: https://github.com/example/phoenix_socket

environment:
  sdk: ^3.11.0

dependencies:
  meta: ^1.9.0
  web_socket_channel: ^3.0.3

dev_dependencies:
  fake_async: ^1.3.1
  stream_channel: ^2.1.2
  test: ^1.25.0
  very_good_analysis: ^10.2.0
